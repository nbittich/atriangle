application:
  basePath: ${PROJECT_BASE_PATH:${java.io.tmpdir}/xls2rdf-rest}
spring:
  application:
    name: Xls2RdfRestPublicEndpoint
  profiles:
    include: kafka, zookeeper, rest, feign, logging
