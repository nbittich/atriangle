application:
  basePath: ${PROJECT_BASE_PATH:${java.io.tmpdir}/elastic-rest}
spring:
  application:
    name: ElasticRestPublicEndpoint
  profiles:
    include: kafka, zookeeper, rest, feign, logging, elasticsearch
