server:
  port: 8088
spring:
  application:
    name: RestPublicEndpoint
  profiles:
    include: kafka, virtuoso, elasticsearch, zuul, zookeeper, rest
  kafka:
    template:
      default-topic: event-sink-rest

elasticsearch:
  index: movies

